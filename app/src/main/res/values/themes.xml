<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.NoteApp" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryVariant">@color/primaryLightColor</item>
        <item name="colorOnPrimary">@color/primaryTextColor</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondaryColor</item>
        <item name="colorSecondaryVariant">@color/secondaryLightColor</item>
        <item name="colorOnSecondary">@color/primaryTextColor</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.NoteApp.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.NoteApp.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar" />

    <style name="Theme.NoteApp.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light" />

    <style name="item_note">
        <item name="android:layout_marginStart">@dimen/primary_margin_and_padding</item>
        <item name="android:layout_marginEnd">@dimen/primary_margin_and_padding</item>
        <item name="android:layout_marginTop">@dimen/primary_margin_and_padding</item>
        <item name="cardBackgroundColor">@color/primaryLightColor</item>
    </style>

    <style name="item_note.primary">
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="item_note.secondary">
        <item name="android:textSize">12sp</item>
    </style>
</resources>