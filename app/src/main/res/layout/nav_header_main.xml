<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="@dimen/drawer_menu_header_height"
    android:background="@drawable/ic_launcher_background"
    android:padding="@dimen/primary_margin_and_padding"
    android:theme="@style/ThemeOverlay.AppCompat">

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/account_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_baseline_sports_basketball_56"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/account_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/drawer_menu_username"
        app:layout_constraintBottom_toTopOf="@id/account_email"
        app:layout_constraintStart_toEndOf="@id/account_image"
        app:layout_constraintTop_toTopOf="@id/account_image" />

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/account_email"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/drawer_menu_user_email"
        app:layout_constraintBottom_toBottomOf="@id/account_image"
        app:layout_constraintStart_toEndOf="@id/account_image"
        app:layout_constraintTop_toBottomOf="@+id/account_name" />

</androidx.constraintlayout.widget.ConstraintLayout>